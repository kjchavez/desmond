syntax = "proto3";

package desmond.network;

message Beacon {
  // Unique identifier for this network node.
  string uuid = 1;

  // Port to which a SUB can connect.
  int32 port = 2;

  enum NodeType {
    UNKNOWN = 0;
    PUB = 1;
  }
  NodeType node_type = 3;
}
